@extends('layouts.app')
@section('content')<div class="page-head"><div><div class="eyebrow">TRANSPORTATION RECORDS</div><h1>Operators</h1><p class="muted">Manage registered transport operators and their records.</p></div>@if(auth()->user()->canManage())<a href="{{ route('operators.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i> Add operator</a>@endif</div><div class="panel"><form class="row g-2 mb-4"><div class="col-md-5"><input class="form-control" name="search" placeholder="Search name, code, or contact..." value="{{ request('search') }}"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">All statuses</option><option value="active" @selected(request('status')==='active')>Active</option><option value="inactive" @selected(request('status')==='inactive')>Inactive</option></select></div><div class="col-auto"><button class="btn btn-light border"><i class="bi bi-search"></i> Search</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Operator</th><th>Code</th><th>Contact</th><th>Vehicles</th><th>Status</th><th></th></tr></thead><tbody>@forelse($operators as $operator)<tr><td><a class="fw-semibold text-decoration-none" href="{{ route('operators.show',$operator) }}">{{ $operator->full_name }}</a><small class="d-block text-muted">{{ $operator->email ?: 'No email' }}</small></td><td><span class="text-muted">{{ $operator->operator_code }}</span></td><td>{{ $operator->contact_number }}</td><td>{{ $operator->vehicles()->count() }}</td><td><span class="badge {{ $operator->status==='active' ? 'bg-success-subtle text-success' : 'bg-secondary-subtle text-secondary' }}">{{ ucfirst($operator->status) }}</span></td><td class="text-end"><a href="{{ route('operators.show',$operator) }}" class="btn btn-sm btn-light border"><i class="bi bi-arrow-up-right"></i></a></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">No operators found.</td></tr>@endforelse</tbody></table></div>{{ $operators->links() }}</div>@endsection
