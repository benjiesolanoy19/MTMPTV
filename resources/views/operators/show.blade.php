@extends('layouts.app')
@section('content')<div class="page-head"><div><a href="{{ route('operators.index') }}" class="text-link"><i class="bi bi-arrow-left"></i> Operators</a><h1>{{ $operator->full_name }}</h1><p class="muted">{{ $operator->operator_code }} · {{ ucfirst($operator->status) }} operator</p></div><div class="page-actions">@if(auth()->user()->canManage())<a href="{{ route('operators.edit',$operator) }}" class="btn btn-primary"><i class="bi bi-pencil me-1"></i> Edit record</a>@endif</div></div><div class="row g-4"><div class="col-lg-4"><div class="panel"><h3 class="mb-3">Personal information</h3><dl class="row small"><dt class="col-5 text-muted">Address</dt><dd class="col-7">{{ $operator->address }}</dd><dt class="col-5 text-muted">Contact</dt><dd class="col-7">{{ $operator->contact_number }}</dd><dt class="col-5 text-muted">Email</dt><dd class="col-7">{{ $operator->email ?: '—' }}</dd><dt class="col-5 text-muted">Valid ID</dt><dd class="col-7">{{ $operator->valid_id_type ? $operator->valid_id_type.' · '.$operator->valid_id_number : '—' }}</dd></dl></div></div><div class="col-lg-8"><div class="panel"><div class="panel-head"><div><h3>Associated vehicles</h3><p class="muted">Vehicles registered under this operator</p></div><span class="badge bg-light text-dark">{{ $operator->vehicles->count() }} records</span></div><div class="table-responsive"><table class="table"><thead><tr><th>Vehicle code</th><th>Plate number</th><th>Type</th><th>Status</th></tr></thead><tbody>@forelse($operator->vehicles as $vehicle)<tr><td>{{ $vehicle->vehicle_code }}</td><td>{{ $vehicle->plate_number }}</td><td>{{ $vehicle->vehicle_type }}</td><td>{{ ucfirst($vehicle->status) }}</td></tr>@empty<tr><td colspan="4" class="text-muted">No vehicles registered.</td></tr>@endforelse</tbody></table></div></div></div></div>@endsection
