@extends('layouts.app')
@section('content')<div class="page-head"><div><div class="eyebrow">TRANSPORTATION RECORDS</div><h1>Vehicles</h1><p class="muted">Maintain the municipal fleet registry.</p></div>@if(auth()->user()->canManage())<a href="{{ route('vehicles.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i> Add vehicle</a>@endif</div><div class="panel"><form class="row g-2 mb-4"><div class="col-md-5"><input class="form-control" name="search" placeholder="Search plate, code, or operator..." value="{{ request('search') }}"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['active','inactive','impounded'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-light border"><i class="bi bi-search"></i> Search</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Vehicle</th><th>Operator</th><th>Plate number</th><th>Registration expiry</th><th>Status</th><th></th></tr></thead><tbody>@forelse($vehicles as $vehicle)<tr><td><a class="fw-semibold text-decoration-none" href="{{ route('vehicles.show',$vehicle) }}">{{ $vehicle->vehicle_code }}</a><small class="d-block text-muted">{{ $vehicle->vehicle_type }} · {{ $vehicle->make }} {{ $vehicle->model }}</small></td><td>{{ $vehicle->operator->full_name }}</td><td>{{ $vehicle->plate_number }}</td><td>{{ $vehicle->registration_expiry?->format('d M Y') ?: '—' }}</td><td><span class="badge bg-success-subtle text-success">{{ ucfirst($vehicle->status) }}</span></td><td><a href="{{ route('vehicles.show',$vehicle) }}" class="btn btn-sm btn-light border"><i class="bi bi-arrow-up-right"></i></a></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">No vehicles found.</td></tr>@endforelse</tbody></table></div>{{ $vehicles->links() }}</div>@endsection
