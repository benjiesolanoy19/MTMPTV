@extends('layouts.app')
@section('content')<div class="page-head"><div><div class="eyebrow">REPORTING</div><h1>Reports</h1><p class="muted">Search public report records and view their current status.</p></div><a href="{{ route('reports.mine') }}" class="btn btn-light border"><i class="bi bi-person-lines-fill me-1"></i> My reports</a></div><div class="panel"><form class="row g-2 mb-4"><div class="col-md-4"><input name="search" class="form-control" placeholder="Search report, type, or location..." value="{{ request('search') }}"></div><div class="col-md-2"><select name="status" class="form-select"><option value="">All statuses</option>@foreach(['Submitted','Pending','Under Review','Resolved','Closed'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></div><div class="col-md-2"><input type="date" name="date_from" class="form-control" value="{{ request('date_from') }}" aria-label="Date from"></div><div class="col-md-2"><input type="date" name="date_to" class="form-control" value="{{ request('date_to') }}" aria-label="Date to"></div><div class="col-auto"><button class="btn btn-light border"><i class="bi bi-search"></i> Search</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Report ID</th><th>Date</th><th>Report type</th><th>Location</th><th>Status</th><th></th></tr></thead><tbody>@forelse($reports as $report)<tr><td class="fw-semibold">{{ $report->report_number }}</td><td>{{ $report->date_submitted->format('d M Y') }}</td><td>{{ $report->report_type }}</td><td>{{ $report->location ?: '—' }}</td><td><span class="badge bg-light text-dark">{{ $report->status }}</span></td><td><a href="{{ route('reports.show',$report) }}" class="btn btn-sm btn-light border"><i class="bi bi-eye"></i> View details</a></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">No reports found.</td></tr>@endforelse</tbody></table></div>{{ $reports->links() }}</div>@endsection
