@extends('layouts.app')
@section('content')<div class="page-head"><div><div class="eyebrow">ACCOUNT</div><h1>Notifications</h1><p class="muted">Updates relevant to your account and reports.</p></div></div><div class="panel">@forelse($notifications as $notification)<div class="alert-row"><div><strong>{{ $notification->title }}</strong><small>{{ $notification->message }}</small><small>{{ $notification->created_at->diffForHumans() }}</small></div>@if(!$notification->read_at)<form method="POST" action="{{ route('notifications.read',$notification->id) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-light border">Mark read</button></form>@endif</div>@empty<div class="empty"><i class="bi bi-bell-slash"></i><span>No notifications yet.</span></div>@endforelse{{ $notifications->links() }}</div>@endsection
