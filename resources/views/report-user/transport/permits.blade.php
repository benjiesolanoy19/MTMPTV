@extends('layouts.app')
@section('content')<div class="page-head"><div><div class="eyebrow">TRANSPORT INFORMATION</div><h1>Permits</h1><p class="muted">Public permit records.</p></div></div><div class="panel"><form class="row g-2 mb-4"><div class="col-md-5"><input name="search" class="form-control" placeholder="Search permit or operator..." value="{{ request('search') }}"></div><div class="col-md-3"><select name="status" class="form-select"><option value="">All statuses</option>@foreach(['Active','Expiring Soon','Expired','Suspended','Cancelled'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-light border"><i class="bi bi-search"></i> Search</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Permit number</th><th>Operator</th><th>Issue date</th><th>Expiry date</th><th>Status</th><th></th></tr></thead><tbody>@forelse($permits as $permit)<tr><td>{{ $permit->permit_number }}</td><td>{{ $permit->operator->full_name }}</td><td>{{ $permit->issue_date->format('d M Y') }}</td><td>{{ $permit->expiry_date->format('d M Y') }}</td><td>{{ $permit->computed_status }}</td><td><a href="{{ route('permits.show',$permit) }}" class="btn btn-sm btn-light border"><i class="bi bi-eye"></i> View</a></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">No permit records found.</td></tr>@endforelse</tbody></table></div>{{ $permits->links() }}</div>@endsection
