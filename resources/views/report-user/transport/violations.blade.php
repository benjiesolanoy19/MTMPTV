@extends('layouts.app')
@section('content')<div class="page-head"><div><div class="eyebrow">TRANSPORT INFORMATION</div><h1>Violations</h1><p class="muted">Public violation records without confidential enforcement details.</p></div></div><div class="panel"><form class="row g-2 mb-4"><div class="col-md-5"><input name="search" class="form-control" placeholder="Search violation, type, or location..." value="{{ request('search') }}"></div><div class="col-md-3"><select name="status" class="form-select"><option value="">All statuses</option>@foreach(['Open','Resolved','Dismissed'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-light border"><i class="bi bi-search"></i> Search</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Violation ID</th><th>Type</th><th>Date</th><th>Location</th><th>Status</th><th></th></tr></thead><tbody>@forelse($violations as $violation)<tr><td>{{ $violation->violation_number }}</td><td>{{ $violation->violation_type }}</td><td>{{ $violation->violation_date->format('d M Y') }}</td><td>{{ $violation->location ?: '—' }}</td><td>{{ $violation->status }}</td><td><a href="{{ route('violations.show',$violation) }}" class="btn btn-sm btn-light border"><i class="bi bi-eye"></i> View</a></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">No violation records found.</td></tr>@endforelse</tbody></table></div>{{ $violations->links() }}</div>@endsection
