@extends('layouts.app')
@section('content')<div class="page-head"><div><div class="eyebrow">TRANSPORT INFORMATION</div><h1>Renewals</h1><p class="muted">Public renewal status records.</p></div></div><div class="panel"><form class="row g-2 mb-4"><div class="col-md-5"><input name="search" class="form-control" placeholder="Search renewal or operator..." value="{{ request('search') }}"></div><div class="col-md-3"><select name="status" class="form-select"><option value="">All statuses</option>@foreach(['Pending','Approved','Rejected'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-light border"><i class="bi bi-search"></i> Search</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Renewal number</th><th>Operator</th><th>Date applied</th><th>New expiry</th><th>Status</th><th></th></tr></thead><tbody>@forelse($renewals as $renewal)<tr><td>{{ $renewal->renewal_number }}</td><td>{{ $renewal->operator->full_name }}</td><td>{{ $renewal->date_applied->format('d M Y') }}</td><td>{{ $renewal->new_expiry_date->format('d M Y') }}</td><td>{{ $renewal->status }}</td><td><a href="{{ route('renewals.show',$renewal) }}" class="btn btn-sm btn-light border"><i class="bi bi-eye"></i> View</a></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">No renewal records found.</td></tr>@endforelse</tbody></table></div>{{ $renewals->links() }}</div>@endsection