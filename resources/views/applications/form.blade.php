@extends('layouts.app')
@section('content')<div class="page-head"><div><div class="eyebrow">APPLICATIONS / NEW</div><h1>Submit application</h1><p class="muted">Create a new franchise, permit, or registration application.</p></div></div><div class="panel"><form method="POST" action="{{ route('applications.store') }}">@csrf<div class="row g-3"><div class="col-md-6"><label class="form-label">Operator</label><select name="operator_id" class="form-select" required><option value="">Select operator</option>@foreach($operators as $operator)<option value="{{ $operator->id }}">{{ $operator->full_name }} ({{ $operator->operator_code }})</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Vehicle</label><select name="vehicle_id" class="form-select" required><option value="">Select vehicle</option>@foreach($vehicles as $vehicle)<option value="{{ $vehicle->id }}">{{ $vehicle->plate_number }} · {{ $vehicle->operator->full_name }}</option>@endforeach</select></div><div class="col-md-5"><label class="form-label">Application type</label><select name="application_type" class="form-select" required>@foreach(['New Franchise','Franchise Renewal','New Permit','Permit Renewal','Registration'] as $type)<option>{{ $type }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Date submitted</label><input type="date" name="date_submitted" class="form-control" value="{{ date('Y-m-d') }}" required></div><div class="col-12"><label class="form-label">Supporting remarks</label><textarea name="remarks" class="form-control" rows="4" placeholder="Add relevant notes or document references..."></textarea></div></div><div class="mt-4 d-flex gap-2"><a href="{{ route('applications.index') }}" class="btn btn-light border">Cancel</a><button class="btn btn-primary">Submit application</button></div></form></div>@endsection
