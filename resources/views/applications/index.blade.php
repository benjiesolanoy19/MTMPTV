@extends('layouts.app')
@section('content')<div class="page-head"><div><div class="eyebrow">APPLICATIONS</div><h1>Application desk</h1><p class="muted">Review and process franchise and permit applications.</p></div>@if(auth()->user()->canManage())<a href="{{ route('applications.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i> New application</a>@endif</div><div class="panel"><form class="row g-2 mb-4"><div class="col-md-5"><input class="form-control" name="search" placeholder="Search application number or operator..." value="{{ request('search') }}"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['Pending','Under Review','Approved','Rejected','Cancelled'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-light border"><i class="bi bi-search"></i> Search</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Application</th><th>Applicant</th><th>Vehicle</th><th>Type</th><th>Submitted</th><th>Status</th><th></th></tr></thead><tbody>@forelse($applications as $application)<tr><td><a href="{{ route('applications.show',$application) }}" class="fw-semibold text-decoration-none">{{ $application->application_number }}</a></td><td>{{ $application->operator->full_name }}</td><td>{{ $application->vehicle->plate_number }}</td><td>{{ $application->application_type }}</td><td>{{ $application->date_submitted->format('d M Y') }}</td><td><span class="badge {{ $application->status==='Approved'?'bg-success-subtle text-success':($application->status==='Rejected'?'bg-danger-subtle text-danger':'bg-warning-subtle text-warning-emphasis') }}">{{ $application->status }}</span></td><td><a href="{{ route('applications.show',$application) }}" class="btn btn-sm btn-light border"><i class="bi bi-arrow-up-right"></i></a></td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-muted">No applications found.</td></tr>@endforelse</tbody></table></div>{{ $applications->links() }}</div>@endsection
