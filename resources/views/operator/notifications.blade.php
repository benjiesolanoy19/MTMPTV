@extends('layouts.app')
@section('content')<div class="page-head"><div><div class="eyebrow">ACCOUNT</div><h1>Notifications</h1><p class="muted">Updates relevant to your applications and transport records.</p></div></div><div class="panel">@forelse($notifications as $notification)<div class="alert-row"><div><strong>{{ $notification->title }}</strong><small>{{ $notification->message }}</small><small>{{ $notification->created_at->diffForHumans() }}</small></div></div>@empty<div class="empty"><i class="bi bi-bell-slash"></i><span>No notifications yet.</span></div>@endforelse{{ $notifications->links() }}</div>@endsection
